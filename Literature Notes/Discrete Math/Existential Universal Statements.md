# Existential Universal Statements

Form:

- `∃x ∀y P(x, y)`

Meaning:

- There exists one `x` that works for every `y`.

Example:

- `∃m ∈ ℕ^+ ∀n ∈ ℕ^+ (m ≤ n)`
- This is true because `m = 1` works for all positive `n`.

Note:

- Changing the order to `∀y ∃x P(x, y)` gives a different meaning.